// Re-export from root strings file so feature imports can use the app-relative path.
// When old src/strings.ts is eventually removed, update this to the canonical definition.
export { STRINGS } from '../strings';
