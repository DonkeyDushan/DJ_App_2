/**
 * Canvas colours for the trim waveform. Defined here because canvas fills are
 * imperative strings that cannot be expressed as CSS Module classes.
 */

/** Bar colour for waveform columns inside the kept selection. */
export const WAVEFORM_SELECTED_COLOR = '#6ad7c8';

/** Bar colour for waveform columns outside the kept selection (discarded). */
export const WAVEFORM_UNSELECTED_COLOR = '#433d5e';

/** Baseline/background fill behind the waveform bars. */
export const WAVEFORM_BACKGROUND_COLOR = '#13111f';
